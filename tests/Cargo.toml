[package]
name = "tests"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dev-dependencies]
reqwest = { version = "0.11", features = ["json", "blocking"] }
assert_cmd = "2.0"
glob = "0.3.0"
rsa = "*" # see https://github.com/jedisct1/rust-jwt-simple/issues/64
#restest = "0.1.0"
restest = { git = "https://github.com/iomentum/restest", branch = "zdimension/separate-status-body" }
lazy_static = "1.4.0"
tokio = { version = "1", features = ["full"] }
once_cell = "1.13.0"
static_init = "*"
regex = "*"

# Error handling
#thiserror = "1.0.31"
anyhow = "1.0.58"

[dependencies]
shared = { path = "../shared" }