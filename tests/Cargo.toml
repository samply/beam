[package]
name = "tests"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dev-dependencies]
reqwest = { version = "0.11", features = ["json", "blocking"] }
assert_cmd = "2.0"
glob = "0.3.0"
rsa = "*" # see https://github.com/jedisct1/rust-jwt-simple/issues/64

# Error handling
#thiserror = "1.0.31"
anyhow = "1.0.58"

[dependencies]
shared = { path = "../shared" }